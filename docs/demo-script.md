# Demo Script

Run the facial fatigue demo from the repository root:

```bash
python -m backend.app.ml.demo_facial_fatigue
```

The command prints API-ready JSON for `low_risk`, `moderate_risk`, and `high_risk` scan profiles.
